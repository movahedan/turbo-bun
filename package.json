{
	"name": "kitchen-sink",
	"private": true,
	"scripts": {
		"//setup": "/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////",
		"setup": "bun run scripts/setup/setup.ts",
		"cleanup": "bun run scripts/setup/cleanup.ts",
		"sync:vscode": "bun run scripts/sync-vscode-config.ts",
		"postinstall": "bun run sync:vscode",
		"//biome and checks": "//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////",
		"check": "biome check .",
		"check:fix": "biome check . --write",
		"check:types": "turbo run check:types",
		"check:quick": "bun run check:fix && turbo run check:types --affected && bun run test --affected && bun run build --affected",
		"//turbo": "/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////",
		"dev": "turbo run dev",
		"test": "turbo run test",
		"build": "turbo run build",
		"start": "turbo run start",
		"//devcontainers": "/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////",
		"dev:test-devcontainer": "bun run scripts/setup/test-devcontainer.ts",
		"dev:container": "docker compose -f .devcontainer/docker-compose.dev.yml",
		"dev:status": "bun run dev:container ps",
		"dev:health": "bun run dev:container ps --format table",
		"dev:clean": "bun run dev:container down --remove-orphans --volumes && docker volume prune -f && docker system prune -f",
		"dev:clean:all": "bun run dev:clean && bun run scripts/setup/cleanup.dev.ts",
		"dev:build": "bun run dev:container build --parallel",
		"dev:build:cache": "bun run dev:container build --parallel --no-cache",
		"dev:down": "bun run dev:container --profile all down --remove-orphans",
		"dev:up": "bun run dev:container --profile all up -d",
		"dev:logs": "bun run dev:container logs -f",
		"dev:watch": "bun run dev:container watch",
		"dev:admin:up": "bun run dev:container --profile admin up -d",
		"dev:admin:logs": "bun run dev:container logs -f admin",
		"dev:admin:watch": "bun run dev:container watch admin",
		"dev:blog:up": "bun run dev:container --profile blog up -d",
		"dev:blog:logs": "bun run dev:container logs -f blog",
		"dev:blog:watch": "bun run dev:container watch blog",
		"dev:storefront:up": "bun run dev:container --profile storefront up -d",
		"dev:storefront:logs": "bun run dev:container logs -f storefront",
		"dev:storefront:watch": "bun run dev:container watch storefront",
		"dev:api:up": "bun run dev:container --profile api up -d",
		"dev:api:logs": "bun run dev:container logs -f api",
		"dev:api:watch": "bun run dev:container watch api",
		"//turbo docker optimized": "////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////",
		"dev:turbo:build": "turbo run build --filter=@repo/* && bun run dev:build",
		"dev:turbo:affected": "docker compose -f .devcontainer/docker-compose.dev.yml --profile all up -d --build && turbo run dev --affected",
		"//prodcontainers": "/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////",
		"prod:up": "docker compose -f docker-compose.prod.yml up -d",
		"prod:down": "docker compose -f docker-compose.prod.yml down",
		"prod:build": "docker compose -f docker-compose.prod.yml build",
		"prod:status": "docker compose -f docker-compose.prod.yml ps",
		"//changesets": "/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////",
		"changesets": "bunx --install=force --bun @changesets/cli"
	},
	"devDependencies": {
		"@biomejs/biome": "2.1.2",
		"@changesets/cli": "2.29.5",
		"@commitlint/cli": "19.8.1",
		"@commitlint/config-conventional": "19.8.1",
		"bun-types": "1.2.19",
		"chalk": "5.4.1",
		"lefthook": "1.11.16",
		"turbo": "2.5.5",
		"typescript": "5.8.3"
	},
	"overrides": {
		"@types/react": "18.3.23",
		"@types/react-dom": "18.3.7",
		"typescript": "5.8.3",
		"react": "18.3.1",
		"react-dom": "18.3.1",
		"vite": "6.3.5"
	},
	"packageManager": "bun@1.2.15",
	"workspaces": [
		"apps/*",
		"packages/*"
	]
}
